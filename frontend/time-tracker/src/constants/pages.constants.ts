export const enum PAGES {
    HOME = "/",
    LOGIN = "/login",
    CALENDAR = "/calendar",
    PROFILE = "/profile",
    USERS = "/users",
    TEAMS = "/teams",
	VACATIONS = "/vacations",
	ALL_VACATIONS = "/vacations/all"
}

export const ELEMENTS_ON_PAGE = 10;